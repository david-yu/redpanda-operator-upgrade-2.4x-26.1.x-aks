# Helm values for the operator chart at 25.3.x.
# Same overlay as 25.1.x for this customer — kept in a separate file so
# the Phase 2 ArgoCD diff stays on one Application change (chart version
# + values file pointer).
crds:
  enabled: true

monitoring:
  enabled: true

resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 1,    memory: 512Mi }
