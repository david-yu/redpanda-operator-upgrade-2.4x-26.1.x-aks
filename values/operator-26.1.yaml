# Helm values for the operator chart at 26.1.x.
# Same overlay as 25.3.x for this customer — kept in a separate file so
# the Phase 4 ArgoCD diff stays on one Application change.
crds:
  enabled: true

monitoring:
  enabled: true

resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 1,    memory: 512Mi }
